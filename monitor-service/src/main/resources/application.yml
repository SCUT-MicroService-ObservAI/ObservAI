server:
  port: 8082

spring:
  application:
    name: monitor-service
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDR:localhost:8848}

observai:
  monitor:
    collect-interval-ms: ${MONITOR_COLLECT_INTERVAL_MS:15000}

