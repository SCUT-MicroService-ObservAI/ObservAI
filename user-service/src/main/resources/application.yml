server:
  port: 8081

spring:
  application:
    name: user-service
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDR:localhost:8848}

observai:
  jwt:
    secret: ${JWT_SECRET:observai-default-development-secret-key}
    ttl-seconds: ${JWT_TTL_SECONDS:7200}

